first line befor push on server
second line on the server
yet another line on a NEW BRANCH
@app.route("/get_passwords", methods=["POST"])
def get_passwords():
    category = request.values.get("category")
    data = asdasdfsdfsdfsfd
    return json.dumps({"data": data})